# access-control-service
Backend service responsible for authentication and authorization.This service is designed to issue JWT tokens, support OAuth-based login, and act as a centralized identity provider for internal services.
